# Project instructions

Read `.claude/project-context.md` at the start of each session for this
project's environment, commands, repository layout, and navigation rules.
Public helper settings are generated from `config.nix` into `.nixodoo/`.
Credentials and other runtime settings belong in `.env` or a password file.

Use `.claude/skills/commit/SKILL.md` for Conventional Commit messages.

## Local instructions

Add project-specific instructions here. Framework updates preserve this file.
